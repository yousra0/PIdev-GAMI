{% extends 'base.html.twig' %}

{% block title %}Equipes{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
      <div class="col-lg-12">
        <div class="page-content">
    <h1>Equipes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td style="color: white;">{{ equipe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td style="color: white;">{{ equipe.nom }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td style="color: white;">{{ equipe.logo }}</td>
            </tr>
            <tr>
                <th>Joueurs</th>
                <td style="color: white;">{{ equipe.joueurs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipes_index') }}">back to list</a>

    <a href="{{ path('app_equipes_edit', {'id': equipe.id}) }}">edit</a>

    {{ include('equipes/_delete_form.html.twig') }}
    </div>
    </div>
    </div>
    </div>
{% endblock %}
