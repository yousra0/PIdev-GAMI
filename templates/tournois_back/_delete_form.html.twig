
{% block body %}
<form method="post" action="{{ path('app_tournoisBack_delete', {'id': tournoi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournoi.id) }}">
    <button class="main-border-button">Delete</button>
</form>
{% endblock %}