{% extends 'base2.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
      <div class="col-lg-12">
        <div class="page-content">
<div style >
    <h1>User</h1>

      <table class="table">
        <tbody>
            <tr>
                <th style="color: white;">Email</th>
                <td style="color: white;">{{ user.id }}</td>
            </tr>
            <tr>
                <th style="color: white;">Nom</th>
                <td style="color: white;">{{ user.nom }}</td>
            </tr>
            <tr>
                <th style="color: white;">Prenom</th>
                <td style="color: white;">{{ user.prenom }}</td>
            </tr>
            <tr>
                <th style="color: white;">MDP</th>
                <td style="color: white;">{{ user.MDP }}</td>
            </tr>
            <tr>
                <th style="color: white;">Email</th>
                <td style="color: white;">{{ user.email }}</td>
            </tr>
            <tr>
                <th style="color: white;">Pays</th>
                <td style="color: white;">{{ user.pays }}</td>
            </tr>
            <tr>
                <th style="color: white;">Date</th>
                <td style="color: white;">{{ user.date|date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th style="color: white;">Role</th>
                <td style="color: white;">{{ user.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>
    

    {{ include('user/_delete_form.html.twig') }}
    </div>
        </div>
            </div>
                </div>  
                    </div>
{% endblock %}
